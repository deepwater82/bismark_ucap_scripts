#####################################
README - ucap enable/disable scripts

2011.Dec.2
#####################################


* enable_ucap.sh
  - run when ucap is enabled in UI.

* disable_ucap.sh
  - run when ucap is disabled in UI

* monitor_veth
  - script for monitoring existence of veth devices. 
    This is because veth devices go down when "/etc/init.d/network" is restarted.

* preopenflow.user
  - Script that runs before openflow connects to the controller.

* postopenflow.user
  - Script that runs after openflow connects to the controller.

